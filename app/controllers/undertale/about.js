import Ember from 'ember';

export default Ember.Controller.extend({
});

//$(document).ready(function () {
//    var url = window.location.href;
//    if (url.indexOf("undertale/about")!=-1) {
//        $('.ut_nav').css('display', 'none');
//    } 
//});

//Beta Feature to hide Header while on the About page, and fix changes upon leaving the page.